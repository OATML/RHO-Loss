# @package _global_

defaults:
  - override /model/large_model: cifar_resnet.yaml
  - override /datamodule: cifar10_datamodule.yaml
  - override /irreducible_loss_generator: irreducible_loss_model.yaml
  - override /datamodule/trainset_corruption: label_noise.yaml 

irreducible_loss_generator:
  checkpoint_path: "/users/janner/goldiprox-hydra/outputs/2021-09-29/16-55-44/checkpoints/epoch_007.ckpt"

model:
  percent_train: 0.1

trainer:
  max_epochs: 100

datamodule:
  trainset_corruption:
    pc_corrupted: 0.1
  data_dir: "/scratch-ssd/oatml/data/"
  batch_size: 320

logger:
  wandb:
    project: "jb_cifar10"
