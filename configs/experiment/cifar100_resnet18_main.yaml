# @package _global_
defaults:
  - override /model/large_model: cifar100_resnet.yaml
  - override /datamodule: cifar100_datamodule.yaml
  

model:
  percent_train: 0.1

trainer:
  max_epochs: 175
  gpus: 1

datamodule:
  batch_size: 320

eval_set: test

logger:
  wandb:
    project: "jb-cifar100-full-trainset"