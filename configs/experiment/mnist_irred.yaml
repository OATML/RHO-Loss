# @package _global_

defaults:
  - override /model/model: mnist_mlp512.yaml
  - override /datamodule: qmnist_datamodule.yaml
  - override /callbacks: val_loss.yaml

trainer:
  max_epochs: 100

datamodule:
  data_dir: "/scratch-ssd/oatml/data/"
  batch_size: 320

logger:
  wandb:
    project: "jb_mnist"